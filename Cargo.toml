[package]
name = "tsnet"
version = "0.1.0"
edition = "2024"

[dependencies]
mio = { version = "1.0", features = ["os-ext"] }
libc = "0.2"
tracing = "0.1"

[dev-dependencies]
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

[build-dependencies]
bindgen = "0.71.0"